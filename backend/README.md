# Bubble Agent backend

Run locally:

```powershell
python -m pip install -e ".[dev]"
python -m bubble_agent.main
```

The default `demo` model requires no API key. Configure an OpenAI-compatible provider with
environment variables documented in the repository root README.
